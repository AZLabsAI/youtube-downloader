# YouTube Downloader Environment Variables

# YouTube Cookies (Base64 Encoded)
# Extract cookies from your browser while logged into YouTube
# Base64 encode the cookie file: base64 cookies.txt
# Paste the encoded value here
# Leave empty to run without authentication (some videos may fail)
YTDLP_COOKIES=

# Node Environment
NODE_ENV=production

# Optional: Custom Port (default: 3000)
# PORT=3000
